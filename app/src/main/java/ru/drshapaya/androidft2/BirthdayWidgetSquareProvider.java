package ru.drshapaya.androidft2;

/** One UI 2×2 preset; it uses the regular square-friendly composition. */
public final class BirthdayWidgetSquareProvider extends BirthdayWidgetProvider {
    @Override int presetSize() { return SIZE_SQUARE; }
}
