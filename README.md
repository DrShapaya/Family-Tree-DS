# Family Tree DS

Family Tree DS — нативное Android-приложение для создания и хранения
семейного дерева. Приложение также распространяется под названием AndroidFT.

Текущая версия: **2.6.5**.

## Возможности

- Карточки родственников с фотографиями, датами, местами и заметками.
- Родительские, партнёрские и родственные связи.
- Автоматическое упорядочивание поколений и семейных ветвей.
- Просмотр предков, потомков и близких родственников.
- Определение родства между двумя людьми.
- История действий с Undo/Redo.
- Семейный архив с историями, фотографиями и документами.
- Перенос всего дерева одним файлом `.ftree`.
- Импорт и экспорт JSON и GEDCOM.
- Экспорт в PNG, постраничный PDF и тайловый PNG.
- Светлая, тёмная и чистая темы, компактные карточки и защита правок.
- Необязательные приватные онлайн-деревья через GitHub с ключами приглашений.
- Автоматический русский или английский интерфейс с ручным выбором в параметрах.

Дерево отрисовывается нативным Android Canvas без WebView. Основные данные
хранятся локально на устройстве.

## Требования

- Android 7.0 или новее.
- JDK 17.
- Android SDK 36.

## Debug-сборка

```powershell
.\gradlew.bat assembleDebug
```

APK будет создан в:

```text
app/build/outputs/apk/debug/app-debug.apk
```

## Вход GitHub

Онлайн-раздел открывается пятью нажатиями по номеру версии в верхней части
экрана. В официальной сборке вход выполняется одной кнопкой в браузере GitHub:
после подтверждения пользователь автоматически возвращается в AndroidFT.
Авторизация защищена PKCE и `state`, а полученный токен хранится в Android
Keystore и повторно запрашивается только после выхода или отзыва доступа.

Для собственной сборки или форка OAuth-параметры можно переопределить файлом
`github.properties` по примеру `github.properties.example` либо
Gradle-свойствами `ANDROIDFT_GITHUB_CLIENT_ID` и
`ANDROIDFT_GITHUB_CLIENT_SECRET`. Нативный APK не может скрыть Client Secret от
целенаправленного извлечения, поэтому PKCE остаётся обязательной частью входа.
Онлайн-функции запрашивают области `repo` и `gist`: первая нужна для приватного
дерева, медиа и участников, вторая — для зашифрованного канала приглашений.

Структура дерева синхронизируется через приватный репозиторий, а бинарные
медиафайлы — потоково через служебный GitHub Release этого же приватного
репозитория. Синхронизируются все поддерживаемые приложением фото, документы,
аудио и видео: до 30 МБ для фото, до 100 МБ для обычного вложения и до 500 МБ
для видео. Старые онлайн-деревья с небольшими файлами внутри репозитория
поддерживаются автоматически. При восстановлении структура становится доступна
сразу, а недостающие медиа докачиваются в фоне. Глава может переключить
конкретного участника между редактированием и режимом просмотра.

## Release-сборка

Для локальной подписи создайте файл `keystore.properties` в корне проекта:

```properties
storeFile=C:/path/to/release-key.jks
storePassword=your_store_password
keyAlias=your_key_alias
keyPassword=your_key_password
```

Файл и ключи исключены из Git и не должны попадать в репозиторий.

```powershell
.\gradlew.bat assembleRelease
```

Готовый APK:

```text
app/build/outputs/apk/release/app-release.apk
```

## Данные и конфиденциальность

Приложение не записывает имена людей, заметки или содержимое документов в
диагностический журнал. Перед обновлением старого большого дерева рекомендуется
создать резервную копию или экспортировать файл `.ftree`.
